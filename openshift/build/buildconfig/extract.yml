apiVersion: template.openshift.io/v1
kind: Template

parameters:
- description: Version of Python image to be used (latest for now).
  displayName: Version of Python Image
  name: PYTHON_VERSION
  required: true
  value: latest
- description: Prefix to prepend to object name.
  displayName: Prefix
  name: PREFIX
  required: true
- description: SHA1 of git revision to be deployed.
  displayName: Git SHA1
  name: GIT_SHA1
  required: true

objects:
- apiVersion: build.openshift.io/v1
  kind: BuildConfig
  metadata:
    name: ${PREFIX}extract
  spec:
    runPolicy: Parallel
    nodeSelector:
        beta.kubernetes.io/os: linux
    output:
      to:
        kind: ImageStreamTag
        name: ${PREFIX}extract:${GIT_SHA1}
    resources:
      limits:
        cpu: "1"
        memory: 1Gi
      requests:
        cpu: 100m
        memory: 256Mi
    source:
      git:
        ref: ${GIT_SHA1}
        uri: https://github.com/bcgov/${PREFIX}-extract.git
      contextDir: extract
      type: Git
    strategy:
      sourceStrategy:
        from:
          kind: ImageStreamTag
          name: ${PREFIX}python:${PYTHON_VERSION}
      type: Source
    triggers: []
  status:
    lastVersion: 0
